# Scene V0.1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 造一個 macOS 14+ menu bar app，click 一下就將當前可見窗口排成 7 種 preset layout 之一，帶 global hotkey 同 drag-to-swap。

**Architecture:** Approach B+ — `SceneCore` Swift Package 裝純邏輯（AX wrapper、layout engine、hotkey、drag-swap），`SceneApp` Xcode target 做薄 UI shell（SwiftUI `MenuBarExtra` + onboarding window）。純邏輯全部 `swift test` 測試，冇 Xcode dependency。

**Tech Stack:** Swift 5.9+, SwiftUI, AppKit, ApplicationServices (AX API), Carbon (HotKey), UserNotifications, os.Logger。零外部 dependency。

**Reference Spec:** `docs/superpowers/specs/2026-04-18-scene-design.md`

---

## 實作策略

- 每個 task 都跟 TDD：先寫 failing test → 跑一次確認 fail → 寫最小實作 → 跑一次確認 pass → commit
- 全部純 logic task 用 `swift test` 跑，唔使 Xcode
- 涉及 AX / Carbon / NSWindow 嘅 task 需要 Xcode 或 real permission，會標明
- Commit message 統一用 conventional commits

## Phase Overview

| Phase | Tasks | 目標 |
|---|---|---|
| 1 — Package Scaffold | 1–2 | Package.swift + 空 targets |
| 2 — Pure Logic | 3–8 | Slot / Layout / LayoutEngine.plan()（全部 `swift test`） |
| 3 — System Wrappers | 9–13 | AXPermission / AXWindow / AXWindowEnumerator / ScreenResolver / LayoutEngine.apply() |
| 4 — Interaction | 14–16 | HotkeyManager / nearestSlot / DragSwapController |
| 5 — App Shell | 17–22 | Xcode project / AppDelegate / MenuBarExtra / OnboardingView / Coordinator |
| 6 — Integration | 23–24 | Wiring + manual smoke test |

---

# Phase 1 — Package Scaffold

## Task 1：初始化 Swift Package

**Files:**
- Create: `Package.swift`
- Create: `Sources/SceneCore/SceneCore.swift`
- Create: `Tests/SceneCoreTests/SceneCoreTests.swift`
- Create: `.gitignore`

- [ ] **Step 1：寫 `.gitignore`**

```gitignore
.DS_Store
.build/
.swiftpm/
*.xcodeproj/xcuserdata/
*.xcodeproj/project.xcworkspace/xcuserdata/
DerivedData/
```

- [ ] **Step 2：寫 `Package.swift`**

```swift
// swift-tools-version:5.9
import PackageDescription

let package = Package(
    name: "SceneCore",
    platforms: [.macOS(.v14)],
    products: [
        .library(name: "SceneCore", targets: ["SceneCore"]),
    ],
    targets: [
        .target(
            name: "SceneCore",
            path: "Sources/SceneCore"
        ),
        .testTarget(
            name: "SceneCoreTests",
            dependencies: ["SceneCore"],
            path: "Tests/SceneCoreTests"
        ),
    ]
)
```

- [ ] **Step 3：寫 placeholder source + test**

`Sources/SceneCore/SceneCore.swift`:
```swift
public enum SceneCore {
    public static let version = "0.1.0"
}
```

`Tests/SceneCoreTests/SceneCoreTests.swift`:
```swift
import XCTest
@testable import SceneCore

final class SceneCoreTests: XCTestCase {
    func testVersion() {
        XCTAssertEqual(SceneCore.version, "0.1.0")
    }
}
```

- [ ] **Step 4：Build + test**

Run: `swift build && swift test`
Expected: Build succeed, 1 test pass。

- [ ] **Step 5：Commit**

```bash
git add .gitignore Package.swift Sources/ Tests/
git commit -m "chore: scaffold SceneCore Swift package"
```

---

## Task 2：目錄結構 placeholder

**Files:**
- Create: `Sources/SceneCore/AX/` (empty marker)
- Create: `Sources/SceneCore/Layout/` (empty marker)
- Create: `Sources/SceneCore/Interaction/` (empty marker)
- Create: `Sources/SceneCore/Display/` (empty marker)

- [ ] **Step 1：喺每個子目錄放一個 `.gitkeep` 或者正式嘅 header file**

```bash
mkdir -p Sources/SceneCore/AX Sources/SceneCore/Layout Sources/SceneCore/Interaction Sources/SceneCore/Display
touch Sources/SceneCore/AX/.gitkeep Sources/SceneCore/Layout/.gitkeep Sources/SceneCore/Interaction/.gitkeep Sources/SceneCore/Display/.gitkeep
```

- [ ] **Step 2：確認 build 仲 pass**

Run: `swift build`
Expected: Build succeed。

- [ ] **Step 3：Commit**

```bash
git add Sources/SceneCore
git commit -m "chore: add SceneCore subdirectory structure"
```

---

# Phase 2 — Pure Logic

## Task 3：`Slot` struct

**Files:**
- Create: `Sources/SceneCore/Layout/Slot.swift`
- Create: `Tests/SceneCoreTests/SlotTests.swift`

- [ ] **Step 1：寫 failing test**

`Tests/SceneCoreTests/SlotTests.swift`:
```swift
import XCTest
@testable import SceneCore

final class SlotTests: XCTestCase {
    func testSlotResolvesToAbsoluteRect() {
        let slot = Slot(rect: CGRect(x: 0, y: 0, width: 0.5, height: 1.0))
        let visibleFrame = CGRect(x: 0, y: 24, width: 1920, height: 1056)
        let absolute = slot.absoluteRect(in: visibleFrame)
        XCTAssertEqual(absolute, CGRect(x: 0, y: 24, width: 960, height: 1056))
    }

    func testSlotRejectsOutOfUnitRect() {
        XCTAssertNil(Slot(safe: CGRect(x: 0, y: 0, width: 1.5, height: 1.0)))
        XCTAssertNotNil(Slot(safe: CGRect(x: 0, y: 0, width: 1.0, height: 1.0)))
    }
}
```

- [ ] **Step 2：跑 test 確認 fail**

Run: `swift test --filter SlotTests`
Expected: FAIL（`Slot` 未定義）。

- [ ] **Step 3：寫最小實作**

`Sources/SceneCore/Layout/Slot.swift`:
```swift
import CoreGraphics

public struct Slot: Sendable, Equatable {
    public let rect: CGRect

    public init(rect: CGRect) {
        self.rect = rect
    }

    public init?(safe rect: CGRect) {
        guard rect.minX >= 0, rect.minY >= 0,
              rect.maxX <= 1, rect.maxY <= 1,
              rect.width > 0, rect.height > 0 else { return nil }
        self.rect = rect
    }

    public func absoluteRect(in visibleFrame: CGRect) -> CGRect {
        CGRect(
            x: visibleFrame.minX + rect.minX * visibleFrame.width,
            y: visibleFrame.minY + rect.minY * visibleFrame.height,
            width: rect.width * visibleFrame.width,
            height: rect.height * visibleFrame.height
        )
    }
}
```

- [ ] **Step 4：跑 test 確認 pass**

Run: `swift test --filter SlotTests`
Expected: 2 tests pass。

- [ ] **Step 5：Commit**

```bash
git add Sources/SceneCore/Layout/Slot.swift Tests/SceneCoreTests/SlotTests.swift
git commit -m "feat(core): add Slot with unit-rect semantics"
```

---

## Task 4：`LayoutID` enum + 7 preset 骨架

**Files:**
- Create: `Sources/SceneCore/Layout/Layout.swift`
- Create: `Tests/SceneCoreTests/LayoutTests.swift`

- [ ] **Step 1：寫 failing test（7 個 preset 存在 + slot 數量正確）**

```swift
import XCTest
@testable import SceneCore

final class LayoutTests: XCTestCase {
    func testAllLayoutsPresent() {
        XCTAssertEqual(Layout.all.count, 7)
        XCTAssertEqual(Layout.all.map(\.id), [
            .full, .halves, .thirds, .quads, .mainSide, .leftSplitRight, .leftRightSplit
        ])
    }

    func testSlotCounts() {
        XCTAssertEqual(Layout.full.slots.count, 1)
        XCTAssertEqual(Layout.halves.slots.count, 2)
        XCTAssertEqual(Layout.thirds.slots.count, 3)
        XCTAssertEqual(Layout.quads.slots.count, 4)
        XCTAssertEqual(Layout.mainSide.slots.count, 2)
        XCTAssertEqual(Layout.leftSplitRight.slots.count, 3)
        XCTAssertEqual(Layout.leftRightSplit.slots.count, 3)
    }
}
```

- [ ] **Step 2：跑 test 確認 fail**

Run: `swift test --filter LayoutTests`
Expected: FAIL。

- [ ] **Step 3：寫最小實作**

`Sources/SceneCore/Layout/Layout.swift`:
```swift
import CoreGraphics

public enum LayoutID: String, Sendable, CaseIterable, Codable {
    case full, halves, thirds, quads, mainSide, leftSplitRight, leftRightSplit
}

public struct Layout: Sendable, Identifiable {
    public let id: LayoutID
    public let name: String
    public let slots: [Slot]

    public static let full = Layout(
        id: .full,
        name: "Full",
        slots: [Slot(rect: CGRect(x: 0, y: 0, width: 1, height: 1))]
    )

    public static let halves = Layout(
        id: .halves,
        name: "Halves",
        slots: [
            Slot(rect: CGRect(x: 0,   y: 0, width: 0.5, height: 1)),
            Slot(rect: CGRect(x: 0.5, y: 0, width: 0.5, height: 1)),
        ]
    )

    public static let thirds = Layout(
        id: .thirds,
        name: "Thirds",
        slots: (0..<3).map { i in
            Slot(rect: CGRect(x: CGFloat(i)/3.0, y: 0, width: 1.0/3.0, height: 1))
        }
    )

    public static let quads = Layout(
        id: .quads,
        name: "Quads",
        slots: [
            Slot(rect: CGRect(x: 0,   y: 0,   width: 0.5, height: 0.5)),
            Slot(rect: CGRect(x: 0.5, y: 0,   width: 0.5, height: 0.5)),
            Slot(rect: CGRect(x: 0,   y: 0.5, width: 0.5, height: 0.5)),
            Slot(rect: CGRect(x: 0.5, y: 0.5, width: 0.5, height: 0.5)),
        ]
    )

    public static let mainSide = Layout(
        id: .mainSide,
        name: "Main + Side",
        slots: [
            Slot(rect: CGRect(x: 0,   y: 0, width: 0.7, height: 1)),
            Slot(rect: CGRect(x: 0.7, y: 0, width: 0.3, height: 1)),
        ]
    )

    public static let leftSplitRight = Layout(
        id: .leftSplitRight,
        name: "LeftSplit + Right",
        slots: [
            Slot(rect: CGRect(x: 0,   y: 0,   width: 0.5, height: 0.5)),
            Slot(rect: CGRect(x: 0,   y: 0.5, width: 0.5, height: 0.5)),
            Slot(rect: CGRect(x: 0.5, y: 0,   width: 0.5, height: 1)),
        ]
    )

    public static let leftRightSplit = Layout(
        id: .leftRightSplit,
        name: "Left + RightSplit",
        slots: [
            Slot(rect: CGRect(x: 0,   y: 0,   width: 0.5, height: 1)),
            Slot(rect: CGRect(x: 0.5, y: 0,   width: 0.5, height: 0.5)),
            Slot(rect: CGRect(x: 0.5, y: 0.5, width: 0.5, height: 0.5)),
        ]
    )

    public static let all: [Layout] = [full, halves, thirds, quads, mainSide, leftSplitRight, leftRightSplit]

    public static func layout(for id: LayoutID) -> Layout {
        all.first { $0.id == id }!
    }
}
```

- [ ] **Step 4：跑 test 確認 pass**

Run: `swift test --filter LayoutTests`
Expected: 2 tests pass。

- [ ] **Step 5：加 slot rect 正確性 test**

喺同一個檔：
```swift
func testQuadsOnFullHDVisibleFrame() {
    let visibleFrame = CGRect(x: 0, y: 24, width: 1920, height: 1056)
    let rects = Layout.quads.slots.map { $0.absoluteRect(in: visibleFrame) }
    XCTAssertEqual(rects, [
        CGRect(x: 0,   y: 24,       width: 960, height: 528),
        CGRect(x: 960, y: 24,       width: 960, height: 528),
        CGRect(x: 0,   y: 24 + 528, width: 960, height: 528),
        CGRect(x: 960, y: 24 + 528, width: 960, height: 528),
    ])
}

func testMainSidePercentage() {
    let visibleFrame = CGRect(x: 0, y: 0, width: 1000, height: 1000)
    let rects = Layout.mainSide.slots.map { $0.absoluteRect(in: visibleFrame) }
    XCTAssertEqual(rects[0], CGRect(x: 0,   y: 0, width: 700, height: 1000))
    XCTAssertEqual(rects[1], CGRect(x: 700, y: 0, width: 300, height: 1000))
}
```

- [ ] **Step 6：跑晒 test 確認全部 pass**

Run: `swift test --filter LayoutTests`
Expected: 4 tests pass。

- [ ] **Step 7：Commit**

```bash
git add Sources/SceneCore/Layout/Layout.swift Tests/SceneCoreTests/LayoutTests.swift
git commit -m "feat(core): define LayoutID and 7 preset Layouts"
```

---

## Task 5：`WindowRef` protocol + `MockWindow`

**Files:**
- Create: `Sources/SceneCore/AX/WindowRef.swift`
- Create: `Tests/SceneCoreTests/MockWindow.swift`

- [ ] **Step 1：寫 failing test（mock 可以滿足 protocol）**

`Tests/SceneCoreTests/MockWindow.swift`:
```swift
import Foundation
import CoreGraphics
@testable import SceneCore

final class MockWindow: WindowRef {
    let id: CGWindowID
    let bundleID: String?
    private(set) var frame: CGRect
    var isMinimized: Bool
    var isFullscreen: Bool
    private(set) var setFrameCallCount = 0
    private(set) var minimizeCallCount = 0
    var shouldThrowOnSet: Bool = false

    init(
        id: CGWindowID,
        bundleID: String? = nil,
        frame: CGRect = .zero,
        isMinimized: Bool = false,
        isFullscreen: Bool = false
    ) {
        self.id = id
        self.bundleID = bundleID
        self.frame = frame
        self.isMinimized = isMinimized
        self.isFullscreen = isFullscreen
    }

    func setFrame(_ rect: CGRect) throws {
        setFrameCallCount += 1
        if shouldThrowOnSet { throw MockError.boom }
        frame = rect
    }

    func minimize() throws {
        minimizeCallCount += 1
        isMinimized = true
    }

    enum MockError: Error { case boom }
}

final class MockWindowSmokeTests: XCTestCase {
    func testMockMatchesProtocol() throws {
        let w = MockWindow(id: 1, bundleID: "com.test", frame: .zero)
        try w.setFrame(CGRect(x: 10, y: 10, width: 100, height: 100))
        XCTAssertEqual(w.frame, CGRect(x: 10, y: 10, width: 100, height: 100))
        XCTAssertEqual(w.setFrameCallCount, 1)
        try w.minimize()
        XCTAssertTrue(w.isMinimized)
    }
}
```
（需要 `import XCTest` 同 XCTestCase 類放埋一齊，或者分開。為清晰拆開：MockWindow 放一個 file，smoke test 放 `Tests/SceneCoreTests/MockWindowSmokeTests.swift`。）

- [ ] **Step 2：跑 test 確認 fail（WindowRef 未存在）**

Run: `swift test --filter MockWindowSmokeTests`
Expected: FAIL。

- [ ] **Step 3：寫 `WindowRef` protocol**

`Sources/SceneCore/AX/WindowRef.swift`:
```swift
import CoreGraphics

public protocol WindowRef: AnyObject {
    var id: CGWindowID { get }
    var bundleID: String? { get }
    var frame: CGRect { get }
    var isMinimized: Bool { get }
    var isFullscreen: Bool { get }
    func setFrame(_ rect: CGRect) throws
    func minimize() throws
}
```

- [ ] **Step 4：跑 test 確認 pass**

Run: `swift test --filter MockWindowSmokeTests`
Expected: Pass。

- [ ] **Step 5：Commit**

```bash
git add Sources/SceneCore/AX/WindowRef.swift Tests/SceneCoreTests/MockWindow.swift Tests/SceneCoreTests/MockWindowSmokeTests.swift
git commit -m "feat(core): add WindowRef protocol with MockWindow test double"
```

---

## Task 6：`Plan` + `Outcome` types

**Files:**
- Create: `Sources/SceneCore/Layout/Plan.swift`
- Create: `Tests/SceneCoreTests/PlanTests.swift`

- [ ] **Step 1：寫 failing test**

```swift
import XCTest
@testable import SceneCore

final class PlanTests: XCTestCase {
    func testEmptyPlanIsEmpty() {
        let plan = Plan(placements: [], toMinimize: [], leftEmptySlotCount: 4)
        XCTAssertTrue(plan.isEmpty)
    }

    func testNonEmptyPlan() {
        let plan = Plan(
            placements: [Placement(windowID: 1, targetFrame: .zero)],
            toMinimize: [],
            leftEmptySlotCount: 0
        )
        XCTAssertFalse(plan.isEmpty)
    }
}
```

- [ ] **Step 2：跑 test 確認 fail**

Run: `swift test --filter PlanTests`
Expected: FAIL。

- [ ] **Step 3：寫實作**

`Sources/SceneCore/Layout/Plan.swift`:
```swift
import CoreGraphics

public struct Placement: Sendable, Equatable {
    public let windowID: CGWindowID
    public let targetFrame: CGRect

    public init(windowID: CGWindowID, targetFrame: CGRect) {
        self.windowID = windowID
        self.targetFrame = targetFrame
    }
}

public struct Plan: Sendable, Equatable {
    public let placements: [Placement]
    public let toMinimize: [CGWindowID]
    public let leftEmptySlotCount: Int

    public init(placements: [Placement], toMinimize: [CGWindowID], leftEmptySlotCount: Int) {
        self.placements = placements
        self.toMinimize = toMinimize
        self.leftEmptySlotCount = leftEmptySlotCount
    }

    public var isEmpty: Bool { placements.isEmpty && toMinimize.isEmpty }
}

public enum Outcome: Sendable, Equatable {
    case applied(placed: Int, minimized: Int, leftEmpty: Int, failed: Int)
    case noWindows
}
```

- [ ] **Step 4：跑 test pass**

Run: `swift test --filter PlanTests`
Expected: 2 tests pass。

- [ ] **Step 5：Commit**

```bash
git add Sources/SceneCore/Layout/Plan.swift Tests/SceneCoreTests/PlanTests.swift
git commit -m "feat(core): add Plan/Placement/Outcome data types"
```

---

## Task 7：`LayoutEngine.plan()` — 核心 mapping 邏輯

**Files:**
- Create: `Sources/SceneCore/Layout/LayoutEngine.swift`
- Create: `Tests/SceneCoreTests/LayoutEnginePlanTests.swift`

- [ ] **Step 1：寫 failing test（N = slots：每個窗口入晒 slot）**

```swift
import XCTest
@testable import SceneCore

final class LayoutEnginePlanTests: XCTestCase {
    let vf = CGRect(x: 0, y: 0, width: 1000, height: 1000)

    func testExactMatch() {
        let ws = [
            MockWindow(id: 1, frame: CGRect(x: 0,   y: 0, width: 500, height: 500)),
            MockWindow(id: 2, frame: CGRect(x: 500, y: 0, width: 500, height: 500)),
        ]
        let plan = LayoutEngine.plan(windows: ws, visibleFrame: vf, layout: .halves)
        XCTAssertEqual(plan.placements, [
            Placement(windowID: 1, targetFrame: CGRect(x: 0,   y: 0, width: 500, height: 1000)),
            Placement(windowID: 2, targetFrame: CGRect(x: 500, y: 0, width: 500, height: 1000)),
        ])
        XCTAssertTrue(plan.toMinimize.isEmpty)
        XCTAssertEqual(plan.leftEmptySlotCount, 0)
    }
}
```

- [ ] **Step 2：跑 test 確認 fail**

Run: `swift test --filter LayoutEnginePlanTests`
Expected: FAIL。

- [ ] **Step 3：寫最小 `plan()`**

`Sources/SceneCore/Layout/LayoutEngine.swift`:
```swift
import CoreGraphics

public enum LayoutEngine {
    public static func plan(
        windows: [any WindowRef],
        visibleFrame: CGRect,
        layout: Layout
    ) -> Plan {
        let slotCount = layout.slots.count
        let placedCount = min(windows.count, slotCount)
        let placements: [Placement] = (0..<placedCount).map { i in
            Placement(
                windowID: windows[i].id,
                targetFrame: layout.slots[i].absoluteRect(in: visibleFrame)
            )
        }
        let toMinimize: [CGWindowID] = windows.count > slotCount
            ? windows[slotCount...].map(\.id)
            : []
        let leftEmpty = max(0, slotCount - windows.count)
        return Plan(placements: placements, toMinimize: toMinimize, leftEmptySlotCount: leftEmpty)
    }
}
```

- [ ] **Step 4：跑 test pass**

Run: `swift test --filter LayoutEnginePlanTests`
Expected: 1 test pass。

- [ ] **Step 5：加 N > slots test（多出最小化）**

```swift
func testMoreWindowsThanSlots() {
    let ws = (1...5).map { MockWindow(id: CGWindowID($0)) }
    let plan = LayoutEngine.plan(windows: ws, visibleFrame: vf, layout: .thirds)
    XCTAssertEqual(plan.placements.count, 3)
    XCTAssertEqual(plan.placements.map(\.windowID), [1, 2, 3])
    XCTAssertEqual(plan.toMinimize, [4, 5])
    XCTAssertEqual(plan.leftEmptySlotCount, 0)
}
```

- [ ] **Step 6：加 N < slots test（剩 slot 留空）**

```swift
func testFewerWindowsThanSlots() {
    let ws = (1...2).map { MockWindow(id: CGWindowID($0)) }
    let plan = LayoutEngine.plan(windows: ws, visibleFrame: vf, layout: .quads)
    XCTAssertEqual(plan.placements.count, 2)
    XCTAssertTrue(plan.toMinimize.isEmpty)
    XCTAssertEqual(plan.leftEmptySlotCount, 2)
}
```

- [ ] **Step 7：加 0 windows test**

```swift
func testZeroWindows() {
    let plan = LayoutEngine.plan(windows: [], visibleFrame: vf, layout: .quads)
    XCTAssertTrue(plan.isEmpty)
    XCTAssertEqual(plan.leftEmptySlotCount, 4)
}
```

- [ ] **Step 8：加 z-order frontmost → slot 1 test**

```swift
func testZOrderFrontmostGoesToSlotOne() {
    // windows array 已經係 z-order（caller 責任），所以 index 0 = frontmost
    let front = MockWindow(id: 99)
    let second = MockWindow(id: 7)
    let plan = LayoutEngine.plan(windows: [front, second], visibleFrame: vf, layout: .halves)
    XCTAssertEqual(plan.placements[0].windowID, 99)
    XCTAssertEqual(plan.placements[0].targetFrame.origin.x, 0)  // left slot
}
```

- [ ] **Step 9：跑晒 test**

Run: `swift test --filter LayoutEnginePlanTests`
Expected: 4 tests pass。

- [ ] **Step 10：Commit**

```bash
git add Sources/SceneCore/Layout/LayoutEngine.swift Tests/SceneCoreTests/LayoutEnginePlanTests.swift
git commit -m "feat(core): LayoutEngine.plan maps windows to slots by z-order"
```

---

## Task 8：`rectsApproxEqual` helper + Electron tolerance test

**Files:**
- Create: `Sources/SceneCore/Layout/Geometry.swift`
- Create: `Tests/SceneCoreTests/GeometryTests.swift`

- [ ] **Step 1：寫 failing test**

```swift
import XCTest
@testable import SceneCore

final class GeometryTests: XCTestCase {
    func testApproxEqualWithinTolerance() {
        let a = CGRect(x: 100, y: 100, width: 500, height: 400)
        let b = CGRect(x: 102, y: 98,  width: 503, height: 396)
        XCTAssertTrue(rectsApproxEqual(a, b, tolerance: 5))
    }

    func testApproxEqualBeyondTolerance() {
        let a = CGRect(x: 100, y: 100, width: 500, height: 400)
        let b = CGRect(x: 100, y: 100, width: 500, height: 394) // 6px off
        XCTAssertFalse(rectsApproxEqual(a, b, tolerance: 5))
    }

    func testExactEqualAlwaysPasses() {
        let a = CGRect(x: 0, y: 0, width: 100, height: 100)
        XCTAssertTrue(rectsApproxEqual(a, a, tolerance: 0))
    }
}
```

- [ ] **Step 2：跑確認 fail**

Run: `swift test --filter GeometryTests`
Expected: FAIL。

- [ ] **Step 3：寫實作**

`Sources/SceneCore/Layout/Geometry.swift`:
```swift
import CoreGraphics

public func rectsApproxEqual(_ a: CGRect, _ b: CGRect, tolerance: CGFloat) -> Bool {
    abs(a.minX   - b.minX)   <= tolerance &&
    abs(a.minY   - b.minY)   <= tolerance &&
    abs(a.width  - b.width)  <= tolerance &&
    abs(a.height - b.height) <= tolerance
}
```

- [ ] **Step 4：跑 test pass**

Run: `swift test --filter GeometryTests`
Expected: 3 tests pass。

- [ ] **Step 5：Commit**

```bash
git add Sources/SceneCore/Layout/Geometry.swift Tests/SceneCoreTests/GeometryTests.swift
git commit -m "feat(core): rectsApproxEqual helper for Electron tolerance"
```

---

# Phase 3 — System Wrappers

## Task 9：`AXPermission` wrapper

**Files:**
- Create: `Sources/SceneCore/AX/AXPermission.swift`
- Create: `Tests/SceneCoreTests/AXPermissionTests.swift`

**Note:** `AXIsProcessTrusted()` 依賴進程嘅 AX 權限狀態，難做 unit test。呢度 wrapper 主要保證 API 正確，test 只 compile-check。Integration 喺 manual smoke test 階段驗。

- [ ] **Step 1：寫 API smoke test**

```swift
import XCTest
@testable import SceneCore

final class AXPermissionTests: XCTestCase {
    func testCheckDoesNotCrash() {
        // 只確認 API 唔 crash；實際結果視乎 test runner 有無授權
        _ = AXPermission.check()
    }

    func testSystemSettingsURLIsValid() {
        XCTAssertNotNil(AXPermission.systemSettingsURL)
    }
}
```

- [ ] **Step 2：跑確認 fail**

Run: `swift test --filter AXPermissionTests`
Expected: FAIL。

- [ ] **Step 3：寫實作**

`Sources/SceneCore/AX/AXPermission.swift`:
```swift
import ApplicationServices
import Foundation

public enum AXPermission {
    /// 靜默檢查，唔會彈 prompt
    public static func check() -> Bool {
        AXIsProcessTrusted()
    }

    /// 第一次要彈 system prompt 嗰陣用
    public static func requestWithPrompt() -> Bool {
        let options = [kAXTrustedCheckOptionPrompt.takeUnretainedValue() as String: true]
        return AXIsProcessTrustedWithOptions(options as CFDictionary)
    }

    /// 跳轉去 Privacy → Accessibility pane
    public static let systemSettingsURL = URL(
        string: "x-apple.systempreferences:com.apple.preference.security?Privacy_Accessibility"
    )
}
```

- [ ] **Step 4：跑 test pass**

Run: `swift test --filter AXPermissionTests`
Expected: 2 tests pass。

- [ ] **Step 5：Commit**

```bash
git add Sources/SceneCore/AX/AXPermission.swift Tests/SceneCoreTests/AXPermissionTests.swift
git commit -m "feat(core): AXPermission check + settings URL"
```

---

## Task 10：`AXWindow`（真 AX wrapper）

**Files:**
- Create: `Sources/SceneCore/AX/AXWindow.swift`

**Note:** AX API 要真 AX handle，難寫純 unit test。呢個 task 先 compile-check，真實行為留 integration + manual。

- [ ] **Step 1：寫 `AXWindow`**

```swift
import ApplicationServices
import AppKit
import CoreGraphics

public final class AXWindow: WindowRef {
    public let id: CGWindowID
    public let bundleID: String?
    private let element: AXUIElement
    private let pid: pid_t

    public init(element: AXUIElement, id: CGWindowID, pid: pid_t, bundleID: String?) {
        self.element = element
        self.id = id
        self.pid = pid
        self.bundleID = bundleID
    }

    public var frame: CGRect {
        let pos = axValue(kAXPositionAttribute, type: .cgPoint) as CGPoint? ?? .zero
        let size = axValue(kAXSizeAttribute, type: .cgSize) as CGSize? ?? .zero
        return CGRect(origin: pos, size: size)
    }

    public var isMinimized: Bool {
        (axAttribute(kAXMinimizedAttribute) as? Bool) ?? false
    }

    public var isFullscreen: Bool {
        // kAXFullscreenAttribute 係 private-ish string constant，用字面值
        (axAttributeByName("AXFullScreen") as? Bool) ?? false
    }

    public func setFrame(_ rect: CGRect) throws {
        try setPosition(rect.origin)
        try setSize(rect.size)
    }

    public func minimize() throws {
        let result = AXUIElementSetAttributeValue(element, kAXMinimizedAttribute as CFString, true as CFTypeRef)
        try throwIfFailed(result, op: "minimize")
    }

    // MARK: - private helpers

    private func setPosition(_ point: CGPoint) throws {
        var p = point
        guard let value = AXValueCreate(.cgPoint, &p) else {
            throw AXWindowError.valueCreation
        }
        let result = AXUIElementSetAttributeValue(element, kAXPositionAttribute as CFString, value)
        try throwIfFailed(result, op: "setPosition")
    }

    private func setSize(_ size: CGSize) throws {
        var s = size
        guard let value = AXValueCreate(.cgSize, &s) else {
            throw AXWindowError.valueCreation
        }
        let result = AXUIElementSetAttributeValue(element, kAXSizeAttribute as CFString, value)
        try throwIfFailed(result, op: "setSize")
    }

    private func axAttribute(_ name: String) -> AnyObject? {
        var ref: CFTypeRef?
        guard AXUIElementCopyAttributeValue(element, name as CFString, &ref) == .success else { return nil }
        return ref as AnyObject?
    }

    private func axAttributeByName(_ name: String) -> AnyObject? {
        axAttribute(name)
    }

    private func axValue<T>(_ name: String, type: AXValueType) -> T? {
        guard let raw = axAttribute(name) else { return nil }
        let axVal = raw as! AXValue
        var out = UnsafeMutablePointer<T>.allocate(capacity: 1)
        defer { out.deallocate() }
        guard AXValueGetValue(axVal, type, out) else { return nil }
        return out.pointee
    }

    private func throwIfFailed(_ result: AXError, op: String) throws {
        if result != .success {
            throw AXWindowError.apiFailed(op: op, code: Int(result.rawValue))
        }
    }
}

public enum AXWindowError: Error, Equatable {
    case valueCreation
    case apiFailed(op: String, code: Int)
}
```

- [ ] **Step 2：Build 確認 compile**

Run: `swift build`
Expected: succeed。

- [ ] **Step 3：Commit**

```bash
git add Sources/SceneCore/AX/AXWindow.swift
git commit -m "feat(core): AXWindow wrapper implementing WindowRef"
```

---

## Task 11：`AXWindowEnumerator`

**Files:**
- Create: `Sources/SceneCore/AX/AXWindowEnumerator.swift`

- [ ] **Step 1：寫實作**

```swift
import AppKit
import ApplicationServices
import CoreGraphics

public enum AXWindowEnumerator {
    public enum EnumerationError: Error {
        case permissionDenied
        case cgWindowListFailed
    }

    /// 按 z-order（frontmost first）列出當前 Space 喺 `screen` 上面嘅可見非 minimized、非 fullscreen 窗口
    public static func listVisibleWindows(on screen: NSScreen) throws -> [AXWindow] {
        guard AXPermission.check() else { throw EnumerationError.permissionDenied }

        let options: CGWindowListOption = [.optionOnScreenOnly, .excludeDesktopElements]
        guard let list = CGWindowListCopyWindowInfo(options, kCGNullWindowID) as? [[String: Any]] else {
            throw EnumerationError.cgWindowListFailed
        }

        var results: [AXWindow] = []
        for info in list {
            guard
                let id = info[kCGWindowNumber as String] as? CGWindowID,
                let pid = info[kCGWindowOwnerPID as String] as? pid_t,
                let layer = info[kCGWindowLayer as String] as? Int,
                layer == 0,  // normal app window layer
                let boundsDict = info[kCGWindowBounds as String] as? [String: CGFloat],
                let cgBounds = boundsFromDict(boundsDict)
            else { continue }

            // 以 center 判斷屬邊個 screen
            let center = CGPoint(x: cgBounds.midX, y: cgBounds.midY)
            guard screen.visibleFrame.contains(flip(center, screen: screen)) else { continue }

            let bundleID = runningApp(pid: pid)?.bundleIdentifier
            if let axWindow = buildAXWindow(pid: pid, id: id, bundleID: bundleID, bounds: cgBounds) {
                if !axWindow.isMinimized && !axWindow.isFullscreen {
                    results.append(axWindow)
                }
            }
        }
        return results
    }

    // MARK: - private

    private static func boundsFromDict(_ dict: [String: CGFloat]) -> CGRect? {
        guard
            let x = dict["X"], let y = dict["Y"],
            let w = dict["Width"], let h = dict["Height"]
        else { return nil }
        return CGRect(x: x, y: y, width: w, height: h)
    }

    /// CGWindowList 用 top-left origin（y 向下），NSScreen 用 bottom-left origin（y 向上）
    private static func flip(_ point: CGPoint, screen: NSScreen) -> CGPoint {
        CGPoint(x: point.x, y: screen.frame.maxY - point.y)
    }

    private static func runningApp(pid: pid_t) -> NSRunningApplication? {
        NSRunningApplication(processIdentifier: pid)
    }

    private static func buildAXWindow(pid: pid_t, id: CGWindowID, bundleID: String?, bounds: CGRect) -> AXWindow? {
        let appElement = AXUIElementCreateApplication(pid)
        var windowsRef: CFTypeRef?
        guard AXUIElementCopyAttributeValue(appElement, kAXWindowsAttribute as CFString, &windowsRef) == .success,
              let windows = windowsRef as? [AXUIElement]
        else { return nil }

        // Match by frame equality (best effort — AX 冇直接俾 CGWindowID)
        for window in windows {
            var posRef: CFTypeRef?
            var sizeRef: CFTypeRef?
            AXUIElementCopyAttributeValue(window, kAXPositionAttribute as CFString, &posRef)
            AXUIElementCopyAttributeValue(window, kAXSizeAttribute as CFString, &sizeRef)
            guard let pos = posRef, let size = sizeRef else { continue }

            var point = CGPoint.zero
            var sz = CGSize.zero
            AXValueGetValue(pos as! AXValue, .cgPoint, &point)
            AXValueGetValue(size as! AXValue, .cgSize, &sz)
            let axFrame = CGRect(origin: point, size: sz)

            if rectsApproxEqual(axFrame, bounds, tolerance: 2) {
                return AXWindow(element: window, id: id, pid: pid, bundleID: bundleID)
            }
        }
        return nil
    }
}
```

- [ ] **Step 2：Build 確認 compile**

Run: `swift build`
Expected: succeed。

- [ ] **Step 3：Commit**

```bash
git add Sources/SceneCore/AX/AXWindowEnumerator.swift
git commit -m "feat(core): AXWindowEnumerator lists visible windows on screen"
```

---

## Task 12：`ScreenResolver`

**Files:**
- Create: `Sources/SceneCore/Display/ScreenResolver.swift`

- [ ] **Step 1：寫實作**

```swift
import AppKit
import CoreGraphics

public enum ScreenResolver {
    /// V0.1：滑鼠所在嘅 screen；fallback 到 main screen
    public static func activeScreen() -> NSScreen {
        if let screen = screenContaining(point: NSEvent.mouseLocation) {
            return screen
        }
        return NSScreen.main ?? NSScreen.screens.first!
    }

    public static func screenContaining(point: CGPoint) -> NSScreen? {
        NSScreen.screens.first { $0.frame.contains(point) }
    }

    public static func screenForWindow(_ window: any WindowRef) -> NSScreen? {
        let center = CGPoint(x: window.frame.midX, y: window.frame.midY)
        return screenContaining(point: center)
    }
}
```

- [ ] **Step 2：Build 確認 compile**

Run: `swift build`
Expected: succeed。

- [ ] **Step 3：Commit**

```bash
git add Sources/SceneCore/Display/ScreenResolver.swift
git commit -m "feat(core): ScreenResolver picks active screen from mouse location"
```

---

## Task 13：`LayoutEngine.apply()` + Electron tolerance retry

**Files:**
- Modify: `Sources/SceneCore/Layout/LayoutEngine.swift`
- Create: `Tests/SceneCoreTests/LayoutEngineApplyTests.swift`

- [ ] **Step 1：寫 failing test**

```swift
import XCTest
@testable import SceneCore

final class LayoutEngineApplyTests: XCTestCase {
    func testApplyEmptyPlanReturnsNoWindows() throws {
        let plan = Plan(placements: [], toMinimize: [], leftEmptySlotCount: 4)
        let outcome = try LayoutEngine.apply(plan, on: [])
        XCTAssertEqual(outcome, .noWindows)
    }

    func testApplyPlacesAndMinimizes() throws {
        let w1 = MockWindow(id: 1)
        let w2 = MockWindow(id: 2)
        let w3 = MockWindow(id: 3)
        let plan = Plan(
            placements: [
                Placement(windowID: 1, targetFrame: CGRect(x: 0, y: 0, width: 500, height: 1000)),
                Placement(windowID: 2, targetFrame: CGRect(x: 500, y: 0, width: 500, height: 1000)),
            ],
            toMinimize: [3],
            leftEmptySlotCount: 0
        )
        let outcome = try LayoutEngine.apply(plan, on: [w1, w2, w3])
        XCTAssertEqual(outcome, .applied(placed: 2, minimized: 1, leftEmpty: 0, failed: 0))
        XCTAssertEqual(w1.frame, CGRect(x: 0, y: 0, width: 500, height: 1000))
        XCTAssertEqual(w2.frame, CGRect(x: 500, y: 0, width: 500, height: 1000))
        XCTAssertTrue(w3.isMinimized)
    }

    func testApplyCountsFailures() throws {
        let w1 = MockWindow(id: 1)
        w1.shouldThrowOnSet = true
        let plan = Plan(
            placements: [Placement(windowID: 1, targetFrame: CGRect(x: 0, y: 0, width: 10, height: 10))],
            toMinimize: [],
            leftEmptySlotCount: 0
        )
        let outcome = try LayoutEngine.apply(plan, on: [w1])
        XCTAssertEqual(outcome, .applied(placed: 0, minimized: 0, leftEmpty: 0, failed: 1))
    }
}
```

- [ ] **Step 2：跑確認 fail**

Run: `swift test --filter LayoutEngineApplyTests`
Expected: FAIL。

- [ ] **Step 3：擴充 `LayoutEngine`**

喺 `Sources/SceneCore/Layout/LayoutEngine.swift` 尾加：
```swift
import os

private let log = Logger(subsystem: "com.scene.core", category: "layout-engine")

extension LayoutEngine {
    public static func apply(
        _ plan: Plan,
        on windows: [any WindowRef],
        electronTolerancePx: CGFloat = 5
    ) throws -> Outcome {
        if plan.isEmpty { return .noWindows }

        let byID = Dictionary(uniqueKeysWithValues: windows.map { ($0.id, $0) })
        var placed = 0
        var failed = 0

        for p in plan.placements {
            guard let window = byID[p.windowID] else {
                failed += 1
                continue
            }
            do {
                try window.setFrame(p.targetFrame)
                if !rectsApproxEqual(window.frame, p.targetFrame, tolerance: electronTolerancePx) {
                    let delta = CGSize(
                        width: p.targetFrame.width - window.frame.width,
                        height: p.targetFrame.height - window.frame.height
                    )
                    let corrected = CGRect(
                        x: p.targetFrame.origin.x + (p.targetFrame.origin.x - window.frame.origin.x),
                        y: p.targetFrame.origin.y + (p.targetFrame.origin.y - window.frame.origin.y),
                        width: p.targetFrame.width + delta.width,
                        height: p.targetFrame.height + delta.height
                    )
                    try window.setFrame(corrected)
                }
                placed += 1
            } catch {
                log.error("setFrame failed for \(window.bundleID ?? "unknown", privacy: .public): \(String(describing: error), privacy: .public)")
                failed += 1
            }
        }

        var minimized = 0
        for wid in plan.toMinimize {
            guard let window = byID[wid] else { continue }
            do {
                try window.minimize()
                minimized += 1
            } catch {
                log.error("minimize failed for \(wid): \(String(describing: error), privacy: .public)")
                failed += 1
            }
        }

        return .applied(
            placed: placed,
            minimized: minimized,
            leftEmpty: plan.leftEmptySlotCount,
            failed: failed
        )
    }
}
```

- [ ] **Step 4：跑 test pass**

Run: `swift test --filter LayoutEngineApplyTests`
Expected: 3 tests pass。

- [ ] **Step 5：Commit**

```bash
git add Sources/SceneCore/Layout/LayoutEngine.swift Tests/SceneCoreTests/LayoutEngineApplyTests.swift
git commit -m "feat(core): LayoutEngine.apply with Electron tolerance retry"
```

---

# Phase 4 — Interaction

## Task 14：`HotkeyManager`（Carbon）

**Files:**
- Create: `Sources/SceneCore/Interaction/HotkeyManager.swift`

**Note:** Carbon `RegisterEventHotKey` 需要 real run loop，難純 unit test。呢個 task 先保證 compile + API 形狀正確。真實測試喺 manual smoke。

- [ ] **Step 1：寫實作**

```swift
import Carbon.HIToolbox
import Foundation
import os

public final class HotkeyManager {
    public struct Binding {
        public let id: LayoutID
        public let keyCode: UInt32
        public let modifiers: UInt32
        public let handler: () -> Void
    }

    private var refs: [EventHotKeyRef] = []
    private var handlers: [UInt32: () -> Void] = [:]
    private var nextID: UInt32 = 1
    private var eventHandler: EventHandlerRef?
    private let log = Logger(subsystem: "com.scene.core", category: "hotkey")

    public init() {}

    public func register(
        id: LayoutID,
        keyCode: UInt32,
        modifiers: UInt32,
        handler: @escaping () -> Void
    ) {
        ensureEventHandlerInstalled()

        let hotKeyID = EventHotKeyID(signature: OSType(fourCharCode("SCNE")), id: nextID)
        var ref: EventHotKeyRef?
        let status = RegisterEventHotKey(keyCode, modifiers, hotKeyID, GetApplicationEventTarget(), 0, &ref)

        guard status == noErr, let ref else {
            log.error("RegisterEventHotKey failed for \(id.rawValue, privacy: .public) status=\(status)")
            return
        }

        refs.append(ref)
        handlers[nextID] = handler
        nextID += 1
    }

    public func unregisterAll() {
        for ref in refs { UnregisterEventHotKey(ref) }
        refs.removeAll()
        handlers.removeAll()
    }

    // MARK: - private

    private func ensureEventHandlerInstalled() {
        guard eventHandler == nil else { return }
        var spec = EventTypeSpec(eventClass: OSType(kEventClassKeyboard), eventKind: UInt32(kEventHotKeyPressed))
        let selfPtr = Unmanaged.passUnretained(self).toOpaque()
        InstallEventHandler(
            GetApplicationEventTarget(),
            { _, event, userData -> OSStatus in
                guard let userData = userData, let event = event else { return OSStatus(eventNotHandledErr) }
                var hkID = EventHotKeyID()
                let getStatus = GetEventParameter(
                    event,
                    EventParamName(kEventParamDirectObject),
                    EventParamType(typeEventHotKeyID),
                    nil,
                    MemoryLayout<EventHotKeyID>.size,
                    nil,
                    &hkID
                )
                if getStatus != noErr { return getStatus }
                let manager = Unmanaged<HotkeyManager>.fromOpaque(userData).takeUnretainedValue()
                manager.handlers[hkID.id]?()
                return noErr
            },
            1,
            &spec,
            selfPtr,
            &eventHandler
        )
    }
}

private func fourCharCode(_ s: String) -> UInt32 {
    var result: UInt32 = 0
    for ch in s.utf8.prefix(4) {
        result = (result << 8) | UInt32(ch)
    }
    return result
}

/// Cmd + Shift modifiers（`keyCode` 用 `kVK_ANSI_1` 等）
public enum HotkeyModifiers {
    public static let cmdShift: UInt32 = UInt32(cmdKey | shiftKey)
}

/// ⌘⇧1 … ⌘⇧7 嘅預設 keyCode（ANSI layout）
public enum DefaultHotkeyKeys {
    public static let mapping: [(LayoutID, UInt32)] = [
        (.full,            UInt32(kVK_ANSI_1)),
        (.halves,          UInt32(kVK_ANSI_2)),
        (.thirds,          UInt32(kVK_ANSI_3)),
        (.quads,           UInt32(kVK_ANSI_4)),
        (.mainSide,        UInt32(kVK_ANSI_5)),
        (.leftSplitRight,  UInt32(kVK_ANSI_6)),
        (.leftRightSplit,  UInt32(kVK_ANSI_7)),
    ]
}
```

- [ ] **Step 2：Build 確認 compile**

Run: `swift build`
Expected: succeed。

- [ ] **Step 3：Commit**

```bash
git add Sources/SceneCore/Interaction/HotkeyManager.swift
git commit -m "feat(core): HotkeyManager via Carbon RegisterEventHotKey"
```

---

## Task 15：`nearestSlot` helper（drag-swap 核心）

**Files:**
- Modify: `Sources/SceneCore/Layout/Geometry.swift`
- Create: `Tests/SceneCoreTests/NearestSlotTests.swift`

- [ ] **Step 1：寫 failing test**

```swift
import XCTest
@testable import SceneCore

final class NearestSlotTests: XCTestCase {
    let vf = CGRect(x: 0, y: 0, width: 1000, height: 1000)

    func testNearestSlotTopLeftInQuads() {
        let idx = nearestSlot(to: CGPoint(x: 100, y: 100), layout: .quads, visibleFrame: vf)
        XCTAssertEqual(idx, 0)  // top-left slot
    }

    func testNearestSlotBottomRightInQuads() {
        let idx = nearestSlot(to: CGPoint(x: 900, y: 900), layout: .quads, visibleFrame: vf)
        XCTAssertEqual(idx, 3)  // bottom-right slot
    }

    func testNearestSlotCenterInHalves() {
        // 剛啱喺左右邊界——任何一邊都 OK，實作決定 tiebreak
        let idx = nearestSlot(to: CGPoint(x: 499, y: 500), layout: .halves, visibleFrame: vf)
        XCTAssertEqual(idx, 0)
    }
}
```

- [ ] **Step 2：跑確認 fail**

Run: `swift test --filter NearestSlotTests`
Expected: FAIL。

- [ ] **Step 3：喺 `Geometry.swift` 加：**

```swift
public func nearestSlot(
    to point: CGPoint,
    layout: Layout,
    visibleFrame: CGRect
) -> Int {
    var bestIdx = 0
    var bestDist = CGFloat.infinity
    for (i, slot) in layout.slots.enumerated() {
        let rect = slot.absoluteRect(in: visibleFrame)
        let center = CGPoint(x: rect.midX, y: rect.midY)
        let dx = center.x - point.x
        let dy = center.y - point.y
        let dist = dx * dx + dy * dy
        if dist < bestDist {
            bestDist = dist
            bestIdx = i
        }
    }
    return bestIdx
}
```

- [ ] **Step 4：跑 test pass**

Run: `swift test --filter NearestSlotTests`
Expected: 3 tests pass。

- [ ] **Step 5：Commit**

```bash
git add Sources/SceneCore/Layout/Geometry.swift Tests/SceneCoreTests/NearestSlotTests.swift
git commit -m "feat(core): nearestSlot helper for drag-to-swap"
```

---

## Task 16：`DragSwapController`

**Files:**
- Create: `Sources/SceneCore/Interaction/DragSwapController.swift`

**Note:** 呢個牽涉 AXObserver + global mouse monitor，整體行為只能 manual 測。單元測試 cover `nearestSlot`（Task 15 已做）+ swap logic（內嵌 pure function）。

- [ ] **Step 1：寫 controller**

```swift
import AppKit
import ApplicationServices
import Foundation
import os

public final class DragSwapController {
    public struct Context {
        public let layout: Layout
        public let screen: NSScreen
        public let windows: [any WindowRef]
    }

    /// Caller provides current layout + screen + window list at drag-start time
    private let contextProvider: () -> Context?
    private let preview: DragPreviewOverlay
    private var mouseUpMonitor: Any?
    private var activeDrag: ActiveDrag?
    private var observer: AXObserver?
    private let log = Logger(subsystem: "com.scene.core", category: "drag-swap")

    public init(contextProvider: @escaping () -> Context?) {
        self.contextProvider = contextProvider
        self.preview = DragPreviewOverlay()
    }

    public func start() {
        mouseUpMonitor = NSEvent.addGlobalMonitorForEvents(matching: .leftMouseUp) { [weak self] _ in
            self?.finishDrag()
        }
        // AXObserver 設置留俾 app-level 整合（要 pid 同 element），詳見 Task 23
    }

    public func stop() {
        if let monitor = mouseUpMonitor { NSEvent.removeMonitor(monitor) }
        mouseUpMonitor = nil
        preview.hide()
        activeDrag = nil
    }

    /// 喺 AXObserver callback 入面叫，每次窗口 move 都會觸發
    public func handleWindowMoved(windowID: CGWindowID, currentFrame: CGRect) {
        guard let ctx = contextProvider() else { return }
        guard NSEvent.pressedMouseButtons & 0b1 != 0 else { return }  // 左鍵要按住
        let center = CGPoint(x: currentFrame.midX, y: currentFrame.midY)
        let targetIdx = nearestSlot(to: center, layout: ctx.layout, visibleFrame: ctx.screen.visibleFrame)
        activeDrag = ActiveDrag(windowID: windowID, targetSlotIdx: targetIdx, ctx: ctx)
        preview.show(at: ctx.layout.slots[targetIdx].absoluteRect(in: ctx.screen.visibleFrame))
    }

    private func finishDrag() {
        defer {
            preview.hide()
            activeDrag = nil
        }
        guard let drag = activeDrag else { return }
        let slots = drag.ctx.layout.slots
        let vf = drag.ctx.screen.visibleFrame
        guard let source = drag.ctx.windows.first(where: { $0.id == drag.windowID }) else { return }

        // 揾 target slot 原本邊個窗口
        let targetRect = slots[drag.targetSlotIdx].absoluteRect(in: vf)
        let sourceOriginalSlotIdx = slots.enumerated().first(where: { (_, slot) in
            rectsApproxEqual(slot.absoluteRect(in: vf), source.frame, tolerance: 5)
        })?.offset

        let other = drag.ctx.windows.first { w in
            w.id != source.id && rectsApproxEqual(w.frame, targetRect, tolerance: 5)
        }

        do {
            try source.setFrame(targetRect)
            if let other, let sourceOriginalSlotIdx {
                let otherRect = slots[sourceOriginalSlotIdx].absoluteRect(in: vf)
                try other.setFrame(otherRect)
            }
        } catch {
            log.error("swap failed: \(String(describing: error), privacy: .public)")
        }
    }
}

private struct ActiveDrag {
    let windowID: CGWindowID
    let targetSlotIdx: Int
    let ctx: DragSwapController.Context
}

/// 半透明 overlay 顯示 target slot
@MainActor
private final class DragPreviewOverlay {
    private var window: NSWindow?

    func show(at rect: CGRect) {
        if window == nil {
            let w = NSWindow(
                contentRect: rect,
                styleMask: [.borderless],
                backing: .buffered,
                defer: false
            )
            w.isOpaque = false
            w.backgroundColor = NSColor.systemBlue.withAlphaComponent(0.25)
            w.level = .floating
            w.ignoresMouseEvents = true
            w.hasShadow = false
            window = w
        }
        window?.setFrame(rect, display: true, animate: false)
        window?.orderFront(nil)
    }

    func hide() {
        window?.orderOut(nil)
    }
}
```

- [ ] **Step 2：Build 確認 compile**

Run: `swift build`
Expected: succeed。

- [ ] **Step 3：Commit**

```bash
git add Sources/SceneCore/Interaction/DragSwapController.swift
git commit -m "feat(core): DragSwapController with preview overlay and mouseUp-driven swap"
```

---

# Phase 5 — App Shell

## Task 17：新增 Xcode App Project

**Files:**
- Create: `SceneApp/SceneApp.xcodeproj/`（透過 Xcode GUI 或者 tuist/xcodegen）
- Create: `SceneApp/SceneApp/SceneApp.swift`
- Create: `SceneApp/SceneApp/Info.plist`
- Create: `SceneApp/SceneApp/SceneApp.entitlements`

**推薦用 Xcode 手動建立**（有 GUI，Info.plist 自動生成）。如果想 reproducible，用 `xcodegen`（但係 external dep）。此 plan 用 Xcode 手動。

- [ ] **Step 1：Xcode → File → New → Project → macOS → App**
  - Product Name：`SceneApp`
  - Interface：SwiftUI
  - Language：Swift
  - Use Core Data：❌
  - Include Tests：✅
  - 儲存位置：`SceneApp/`（project root 下）

- [ ] **Step 2：喺 Project settings 加入本地 SceneCore package**
  - Navigator → SceneApp project → Package Dependencies → `+` → Add Local
  - 揀 repo root（即 `/Users/hillmanchan/Desktop/macbook-resizer`）
  - Add `SceneCore` to the `SceneApp` target

- [ ] **Step 3：Info.plist 加 `LSUIElement`**

打開 `SceneApp/SceneApp/Info.plist`（或者 project → Info tab），加：
```xml
<key>LSUIElement</key>
<true/>
```

呢個令 app 冇 Dock icon，純 menu bar。

- [ ] **Step 4：`SceneApp.entitlements` — App Sandbox 關閉**

因為要用 AX API 控制其他 app 嘅窗口，App Sandbox 必須 **disabled**。Entitlements file：
```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>com.apple.security.app-sandbox</key>
    <false/>
</dict>
</plist>
```

Project → Signing & Capabilities → 移走 App Sandbox capability。

- [ ] **Step 5：Build 確認 project 行到**

Xcode：⌘B。
Expected: succeed，app launch 冇 Dock icon。

- [ ] **Step 6：Commit**

```bash
git add SceneApp/
git commit -m "chore: scaffold SceneApp Xcode project with LSUIElement"
```

---

## Task 18：`AppDelegate` + `@main SceneApp`（骨架）

**Files:**
- Modify: `SceneApp/SceneApp/SceneApp.swift`
- Create: `SceneApp/SceneApp/AppDelegate.swift`

- [ ] **Step 1：換掉 Xcode 預設 `SceneApp.swift`**

```swift
import SwiftUI
import SceneCore

@main
struct SceneAppEntry: App {
    @NSApplicationDelegateAdaptor(AppDelegate.self) var delegate

    var body: some Scene {
        MenuBarExtra("Scene", systemImage: "rectangle.3.group") {
            MenuBarContentView()
                .environmentObject(delegate.coordinator)
        }
        .menuBarExtraStyle(.menu)
    }
}
```

- [ ] **Step 2：寫 `AppDelegate.swift`**

```swift
import AppKit
import SwiftUI
import SceneCore

final class AppDelegate: NSObject, NSApplicationDelegate, ObservableObject {
    @Published private(set) var permissionGranted: Bool = false
    lazy var coordinator = Coordinator(onPermissionChange: { [weak self] granted in
        DispatchQueue.main.async {
            self?.permissionGranted = granted
        }
    })

    func applicationDidFinishLaunching(_ notification: Notification) {
        coordinator.start()
    }
}
```

- [ ] **Step 3：placeholder `Coordinator` + `MenuBarContentView` 先 compile**

`SceneApp/SceneApp/Coordinator.swift`:
```swift
import Foundation
import SceneCore

final class Coordinator: ObservableObject {
    private let onPermissionChange: (Bool) -> Void
    init(onPermissionChange: @escaping (Bool) -> Void) {
        self.onPermissionChange = onPermissionChange
    }
    func start() { onPermissionChange(AXPermission.check()) }
    func applyLayout(_ id: LayoutID) {}
}
```

`SceneApp/SceneApp/MenuBarContentView.swift`:
```swift
import SwiftUI

struct MenuBarContentView: View {
    var body: some View {
        Text("Scene — WIP")
        Divider()
        Button("Quit") { NSApp.terminate(nil) }
    }
}
```

- [ ] **Step 4：Build + 跑**

⌘B → ⌘R → 應該 launch 但冇 Dock icon；menu bar 應該有 icon（`rectangle.3.group`）。

- [ ] **Step 5：Commit**

```bash
git add SceneApp/
git commit -m "feat(app): skeleton AppDelegate + MenuBarExtra entry"
```

---

## Task 19：`OnboardingView`（獨立 NSWindow）

**Files:**
- Create: `SceneApp/SceneApp/OnboardingView.swift`
- Create: `SceneApp/SceneApp/OnboardingWindowController.swift`

- [ ] **Step 1：寫 `OnboardingView`**

```swift
import SwiftUI
import SceneCore

struct OnboardingView: View {
    var onGrant: () -> Void

    var body: some View {
        VStack(spacing: 16) {
            Image(systemName: "rectangle.3.group")
                .font(.system(size: 48))
            Text("Scene needs Accessibility access")
                .font(.title2)
                .bold()
            Text("To resize and rearrange your windows, Scene needs permission to control other apps.\n\nClick below to open System Settings, then enable Scene under Privacy & Security → Accessibility.")
                .multilineTextAlignment(.center)
                .frame(maxWidth: 380)
                .foregroundStyle(.secondary)
            Button("Open System Settings") {
                if let url = AXPermission.systemSettingsURL {
                    NSWorkspace.shared.open(url)
                }
            }
            .keyboardShortcut(.defaultAction)
            Button("Check again") { onGrant() }
                .buttonStyle(.link)
        }
        .padding(32)
        .frame(width: 460, height: 340)
    }
}
```

- [ ] **Step 2：寫 window controller**

```swift
import AppKit
import SwiftUI

final class OnboardingWindowController {
    private var window: NSWindow?
    private let onCheck: () -> Void

    init(onCheck: @escaping () -> Void) {
        self.onCheck = onCheck
    }

    @MainActor
    func show() {
        if window == nil {
            let view = OnboardingView(onGrant: onCheck)
            let host = NSHostingController(rootView: view)
            let w = NSWindow(contentViewController: host)
            w.title = "Scene"
            w.styleMask = [.titled, .closable]
            w.center()
            w.isReleasedWhenClosed = false
            window = w
        }
        NSApp.activate(ignoringOtherApps: true)
        window?.makeKeyAndOrderFront(nil)
    }

    @MainActor
    func hide() {
        window?.orderOut(nil)
    }
}
```

- [ ] **Step 3：Build 確認 compile**

⌘B。

- [ ] **Step 4：Commit**

```bash
git add SceneApp/SceneApp/OnboardingView.swift SceneApp/SceneApp/OnboardingWindowController.swift
git commit -m "feat(app): OnboardingView + standalone NSWindow controller"
```

---

## Task 20：`NotificationHelper`（UN + fallback）

**Files:**
- Create: `SceneApp/SceneApp/NotificationHelper.swift`

- [ ] **Step 1：寫實作**

```swift
import AppKit
import UserNotifications

final class NotificationHelper {
    private let statusItem: () -> NSStatusItem?

    init(statusItem: @escaping () -> NSStatusItem?) {
        self.statusItem = statusItem
    }

    func requestAuthorizationIfNeeded() {
        UNUserNotificationCenter.current().requestAuthorization(options: [.alert]) { _, _ in }
    }

    func notifyNoWindows() {
        UNUserNotificationCenter.current().getNotificationSettings { settings in
            DispatchQueue.main.async {
                if settings.authorizationStatus == .authorized {
                    self.postUN(title: "Scene", body: "No windows to arrange")
                } else {
                    self.blinkStatusItem()
                }
            }
        }
    }

    private func postUN(title: String, body: String) {
        let content = UNMutableNotificationContent()
        content.title = title
        content.body = body
        let request = UNNotificationRequest(identifier: UUID().uuidString, content: content, trigger: nil)
        UNUserNotificationCenter.current().add(request)
    }

    @MainActor
    private func blinkStatusItem() {
        guard let item = statusItem(), let button = item.button else { return }
        let oldAlpha = button.alphaValue
        let oldTooltip = button.toolTip
        button.toolTip = "No windows to arrange"
        button.alphaValue = 0.3
        DispatchQueue.main.asyncAfter(deadline: .now() + 0.5) {
            button.alphaValue = oldAlpha
        }
        DispatchQueue.main.asyncAfter(deadline: .now() + 3.0) {
            button.toolTip = oldTooltip
        }
    }
}
```

- [ ] **Step 2：Build 確認 compile**

⌘B。

- [ ] **Step 3：Commit**

```bash
git add SceneApp/SceneApp/NotificationHelper.swift
git commit -m "feat(app): NotificationHelper with UN primary + status item blink fallback"
```

---

## Task 21：`Coordinator`（orchestration 層）

**Files:**
- Modify: `SceneApp/SceneApp/Coordinator.swift`
- Modify: `SceneApp/SceneApp/AppDelegate.swift`

- [ ] **Step 1：重寫 `Coordinator`**

```swift
import AppKit
import SceneCore
import Carbon.HIToolbox
import os

@MainActor
final class Coordinator: ObservableObject {
    @Published private(set) var permissionGranted: Bool = false

    private let log = Logger(subsystem: "com.scene.app", category: "coordinator")
    private let hotkeyManager = HotkeyManager()
    private let onboarding: OnboardingWindowController
    private var notification: NotificationHelper?
    private var permissionPoll: Timer?
    private let onPermissionChange: (Bool) -> Void

    // Status item hook（由 AppDelegate set）
    var statusItem: NSStatusItem?

    init(onPermissionChange: @escaping (Bool) -> Void) {
        self.onPermissionChange = onPermissionChange
        var onboardingRef: OnboardingWindowController? = nil
        self.onboarding = OnboardingWindowController {
            // closure created below captures self after init
        }
        // 配置一次 onCheck
        onboardingRef = self.onboarding  // (placeholder; see note)
        _ = onboardingRef
    }

    func start() {
        self.notification = NotificationHelper { [weak self] in self?.statusItem }
        notification?.requestAuthorizationIfNeeded()
        refreshPermission()
        schedulePermissionPoll()
    }

    func applyLayout(_ id: LayoutID) {
        guard permissionGranted else {
            onboarding.show()
            return
        }
        let screen = ScreenResolver.activeScreen()
        do {
            let windows = try AXWindowEnumerator.listVisibleWindows(on: screen)
            let layout = Layout.layout(for: id)
            let plan = LayoutEngine.plan(
                windows: windows,
                visibleFrame: screen.visibleFrame,
                layout: layout
            )
            let outcome = try LayoutEngine.apply(plan, on: windows)
            if case .noWindows = outcome {
                notification?.notifyNoWindows()
            } else {
                log.info("applied \(id.rawValue, privacy: .public) outcome=\(String(describing: outcome), privacy: .public)")
            }
        } catch AXWindowEnumerator.EnumerationError.permissionDenied {
            setPermission(false)
            onboarding.show()
        } catch {
            log.error("applyLayout error: \(String(describing: error), privacy: .public)")
        }
    }

    // MARK: - permission lifecycle

    private func refreshPermission() {
        setPermission(AXPermission.check())
    }

    private func setPermission(_ granted: Bool) {
        guard granted != permissionGranted else { return }
        permissionGranted = granted
        onPermissionChange(granted)
        if granted {
            registerHotkeys()
            onboarding.hide()
        } else {
            hotkeyManager.unregisterAll()
        }
    }

    private func schedulePermissionPoll() {
        permissionPoll?.invalidate()
        permissionPoll = Timer.scheduledTimer(withTimeInterval: 2.0, repeats: true) { [weak self] _ in
            Task { @MainActor in self?.refreshPermission() }
        }
    }

    private func registerHotkeys() {
        hotkeyManager.unregisterAll()
        for (layoutID, keyCode) in DefaultHotkeyKeys.mapping {
            hotkeyManager.register(
                id: layoutID,
                keyCode: keyCode,
                modifiers: HotkeyModifiers.cmdShift,
                handler: { [weak self] in
                    Task { @MainActor in self?.applyLayout(layoutID) }
                }
            )
        }
    }
}
```

**Note：** 上面 `init` 嘅 closure capture 寫得笨，simplify 如下：

```swift
init(onPermissionChange: @escaping (Bool) -> Void) {
    self.onPermissionChange = onPermissionChange
    // Onboarding 嘅 onCheck 要延遲綁（self reference）
    // 用 weak-capturing closure 避免 retain cycle
    self.onboarding = OnboardingWindowController(onCheck: { })
    self.onboarding.replaceOnCheck { [weak self] in
        Task { @MainActor in self?.refreshPermission() }
    }
}
```

係咁要喺 `OnboardingWindowController` 加：
```swift
func replaceOnCheck(_ new: @escaping () -> Void) {
    // 用一個 mutable closure holder 儲住
}
```

實際簡化方法：將 `onCheck` 改做 var property。更新 `OnboardingWindowController`：

```swift
final class OnboardingWindowController {
    private var window: NSWindow?
    var onCheck: () -> Void = {}

    @MainActor
    func show() { /* same, but use onCheck */ }
    @MainActor
    func hide() { window?.orderOut(nil) }
}
```

同時修改 `OnboardingView`：改用 binding 俾 controller 去設。直接最簡單：

- `OnboardingWindowController` 持 `var onCheck: () -> Void = {}`
- `OnboardingView(onGrant: { [weak self] in self?.onCheck() })`

跟住 `Coordinator.init` 可以：
```swift
self.onboarding = OnboardingWindowController()
self.onboarding.onCheck = { [weak self] in
    Task { @MainActor in self?.refreshPermission() }
}
```

- [ ] **Step 2：Update `OnboardingWindowController` 用 mutable `onCheck`**

```swift
final class OnboardingWindowController {
    private var window: NSWindow?
    var onCheck: () -> Void = {}

    @MainActor
    func show() {
        if window == nil {
            let view = OnboardingView(onGrant: { [weak self] in self?.onCheck() })
            let host = NSHostingController(rootView: view)
            let w = NSWindow(contentViewController: host)
            w.title = "Scene"
            w.styleMask = [.titled, .closable]
            w.center()
            w.isReleasedWhenClosed = false
            window = w
        }
        NSApp.activate(ignoringOtherApps: true)
        window?.makeKeyAndOrderFront(nil)
    }

    @MainActor
    func hide() { window?.orderOut(nil) }
}
```

- [ ] **Step 3：Simplify `Coordinator.init`**

```swift
init(onPermissionChange: @escaping (Bool) -> Void) {
    self.onPermissionChange = onPermissionChange
    self.onboarding = OnboardingWindowController()
    self.onboarding.onCheck = { [weak self] in
        Task { @MainActor in self?.refreshPermission() }
    }
}
```

- [ ] **Step 4：Build + 跑**

⌘B → ⌘R。
Expected: App 行到；撤銷 AX permission 後，再 trigger applyLayout 會彈 onboarding window。

- [ ] **Step 5：Commit**

```bash
git add SceneApp/SceneApp/Coordinator.swift SceneApp/SceneApp/OnboardingWindowController.swift SceneApp/SceneApp/AppDelegate.swift
git commit -m "feat(app): Coordinator wires permission gate, hotkeys, and applyLayout"
```

---

## Task 22：完整 `MenuBarContentView`

**Files:**
- Modify: `SceneApp/SceneApp/MenuBarContentView.swift`

- [ ] **Step 1：完整 view**

```swift
import SwiftUI
import SceneCore

struct MenuBarContentView: View {
    @EnvironmentObject var coordinator: Coordinator

    var body: some View {
        if coordinator.permissionGranted {
            grantedMenu
        } else {
            ungrantedMenu
        }
    }

    private var grantedMenu: some View {
        Group {
            ForEach(Array(Layout.all.enumerated()), id: \.element.id) { (i, layout) in
                Button("\(layout.name)\t⌘⇧\(i+1)") {
                    coordinator.applyLayout(layout.id)
                }
            }
            Divider()
            Button("Quit Scene") { NSApp.terminate(nil) }
        }
    }

    private var ungrantedMenu: some View {
        Group {
            Button("Grant Accessibility Access…") {
                coordinator.openOnboarding()
            }
            Divider()
            Button("Quit Scene") { NSApp.terminate(nil) }
        }
    }
}
```

- [ ] **Step 2：`Coordinator.openOnboarding()` method**

喺 `Coordinator` 加：
```swift
func openOnboarding() {
    onboarding.show()
}
```

- [ ] **Step 3：Build + 手動試**

⌘B → ⌘R。
Expected:
- 第一次開：dropdown 得「Grant Accessibility Access…」+ Quit
- 授權後 dropdown 有 7 個 preset

- [ ] **Step 4：Commit**

```bash
git add SceneApp/SceneApp/MenuBarContentView.swift SceneApp/SceneApp/Coordinator.swift
git commit -m "feat(app): MenuBarContentView renders preset list or grant CTA"
```

---

# Phase 6 — Integration

## Task 23：Drag-swap 整合入 App（AXObserver wiring）

**Files:**
- Create: `SceneApp/SceneApp/DragSwapHost.swift`
- Modify: `SceneApp/SceneApp/Coordinator.swift`

**Note:** AXObserver 要對每個要 observe 嘅 app pid 開一個 observer；V0.1 實作策略：apply layout 之後，對剛剛 placed 嘅 windows 對應嘅 pid 建 observer，listen `kAXMovedNotification`。

- [ ] **Step 1：寫 `DragSwapHost`**

```swift
import AppKit
import ApplicationServices
import SceneCore
import os

@MainActor
final class DragSwapHost {
    private var observers: [pid_t: AXObserver] = [:]
    private var trackedElements: Set<AXUIElementWrapper> = []
    private var currentLayout: Layout?
    private var currentScreen: NSScreen?
    private let controller: DragSwapController
    private let log = Logger(subsystem: "com.scene.app", category: "drag-swap-host")

    init(controller: DragSwapController) {
        self.controller = controller
    }

    func track(windows: [AXWindow], layout: Layout, screen: NSScreen) {
        stopTracking()
        currentLayout = layout
        currentScreen = screen

        let byPID = Dictionary(grouping: windows, by: { $0.pidAccessor })
        for (pid, pidWindows) in byPID {
            var obs: AXObserver?
            AXObserverCreate(pid, { _, _, _, _ in
                // callback 再喺 instance method 處理
            }, &obs)
            // ... 實作省略，因為 AXObserverCreate 需要 C callback；見 workaround 下面
        }
    }

    func stopTracking() {
        observers.removeAll()
        trackedElements.removeAll()
    }
}

// AXObserver callback workaround：用 CFRunLoop 同 context pointer
// V0.1 可以簡化：only observe active screen's placed windows；觸發 handler 喺 main thread
```

**實際 V0.1 建議：** AXObserver C callback 要 bridging，比較煩。**建議 V0.1 簡化：** drag-swap 暫 disable，V0.2 先加。Plan 裡頭呢個 task 標為 **stretch goal**。

- [ ] **Step 2：Decision — 跳過或者做簡化版**

選擇 A：**V0.1 跳過 drag-swap**，`DragSwapController` 編譯 pass 但唔接上，menu bar / hotkey 先 ship。Spec 已經有 note「V0.2 add animation」——drag-swap V0.2 同時做可以。

選擇 B：**V0.1 做簡化版 — 用 NSEvent mouseDown/mouseDragged/mouseUp global monitor 代替 AXObserver**，精度冇 AX 咁準但夠用。

實作者選擇時：如果 schedule 緊，揀 A；想 ship V0.1 完整就揀 B。

- [ ] **Step 3：Commit placeholder**

```bash
git add SceneApp/SceneApp/DragSwapHost.swift
git commit -m "feat(app): DragSwapHost stub — drag-swap deferred to V0.2"
```

---

## Task 24：Manual Smoke Test 跑一次

**Files:**
- Create: `docs/superpowers/manual-tests/v0.1-smoke.md`

- [ ] **Step 1：寫 manual test checklist**

```markdown
# Scene V0.1 Manual Smoke Test

建於 real user machine 跑，macOS 14+。

## Prerequisites
- [ ] 清除 Scene 嘅 Accessibility permission（System Settings → Privacy & Security → Accessibility → 移除 Scene）
- [ ] 開 3 個 TextEdit 窗口

## Tests

- [ ] **P1：第一次啟動 onboarding**
  - 開 Scene → OnboardingView 彈出
  - Click「Open System Settings」→ 跳 Privacy → Accessibility pane
  - 加 Scene → 打 tick
  - 返 Scene → 2 秒內 icon 變正常色，onboarding 自動關閉

- [ ] **P2：基本 preset**
  - Click menu bar Scene icon → 7 個 preset 顯示
  - Click「Halves」→ 3 個 TextEdit 前 2 個左右分；第 3 個 minimize
  - Click「Thirds」→ 前 3 個左中右分

- [ ] **P3：Hotkey**
  - 按 ⌘⇧1 → Full；頭一個 TextEdit 佔全屏
  - 按 ⌘⇧4 → Quads；前 4 個 TextEdit 分 2x2

- [ ] **P4：少窗口**
  - 閂到剩 2 個 TextEdit
  - 按 ⌘⇧4 → 前 2 個 slot 有窗口，其他 slot 空

- [ ] **P5：零窗口**
  - 閂晒所有 TextEdit
  - 按 ⌘⇧2 → 彈 UN notification「No windows to arrange」
  - 若 UN permission 未授權：icon 閃 0.5s + tooltip 變 3s

- [ ] **P6：權限撤銷**
  - System Settings → 關 Scene 嘅 AX permission
  - 2 秒內 menu bar icon 變灰
  - Click icon → dropdown 得「Grant Accessibility Access…」
  - 重新授權 → icon 正常

- [ ] **P7：Electron tolerance**
  - 開 VS Code（或 Cursor），加 2 個 TextEdit
  - Click Halves → 所有 window 喺目標 rect ±5px 內

- [ ] **P8：Multi-display（若有外接 mon）**
  - 滑鼠移去 external mon，click preset → 只影響 external mon 嘅窗口；internal mon 唔郁

- [ ] **P9：visibleFrame**
  - Dock 開住，click Full → 窗口唔壓到 Dock 下面，冇 overlap menu bar
```

- [ ] **Step 2：Commit**

```bash
git add docs/superpowers/manual-tests/v0.1-smoke.md
git commit -m "docs: add V0.1 manual smoke test checklist"
```

- [ ] **Step 3：實際跑一次 smoke test**

按 checklist 逐條做，fail 嘅 log issue / fix。

---

# End State

- Phase 1–4 全部 `swift test` 可以跑，冇 Xcode 都 OK
- Phase 5–6 需要 Xcode build + real AX permission
- Drag-to-swap V0.1 defer（選項 A）或用 NSEvent 簡化版（選項 B），睇 Task 23 決定
- 所有決定跟返 spec `docs/superpowers/specs/2026-04-18-scene-design.md`

---

## Self-Review Notes

**Spec coverage checked against spec `2026-04-18-scene-design.md`：**
- §2 7 個 preset → Task 4
- §3.1 overflow minimize → Task 7 (`testMoreWindowsThanSlots`)
- §3.2 underflow empty slots → Task 7 (`testFewerWindowsThanSlots`)
- §3.3 active screen → Task 12 + Task 21
- §3.4 drag-swap → Task 15 + 16 + 23（V0.1 可 defer）
- §3.5 current Space → Task 11（`CGWindowListCopyWindowInfo` + layer==0 filter）
- §3.6 no animation → Task 13 冇 animation
- §3.7 no windows / single window → Task 7 + Task 21 + Task 20
- §3.8 no settings → Task 22（menu 淨係 preset + Quit）
- §3.9 permission UX → Task 9 + Task 19 + Task 21
- §5 core components → Task 3–16
- §6 data flow → Task 21
- §7 error handling → Task 13 (Electron), Task 21 (permission gate), Task 20 (notification fallback)
- §8 testing → Task 3–8 covered unit tests

**Known intentional simplifications:**
- Task 23（drag-swap app wiring）標為 stretch，因為 AX observer C bridging 煩，V0.1 可 defer
- AXWindow integration test 依賴 Xcode + real AX，冇放 `swift test`

**Placeholder scan passed:** 每個 step 都有具體 code / command / expected output。
